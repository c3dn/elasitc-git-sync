# PocketBase Encryption Key (generate a random string)
ENCRYPTION_KEY=your-secret-encryption-key-change-this

# Public URLs (adjust if deploying to production)
PUBLIC_POCKETBASE_URL=http://localhost:8090
ORIGIN=http://localhost:3000

# SSL/TLS Settings
# Set to "true" to disable SSL certificate verification for all backend HTTP calls
# (useful for environments with self-signed or custom CA certificates)
DISABLE_SSL_VERIFY=false
